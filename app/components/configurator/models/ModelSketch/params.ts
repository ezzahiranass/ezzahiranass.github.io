'use client';

import type { ParamValues } from '../../types';

export const modelSketchDefaults: ParamValues = {
  towerType: 'Twist',
  bevel: 0.2,
  ghostSubdivisions: 5,
  floorHeight: 3.2,
  floors: 10,
  twist: 0.5,
  scale: 0.5,
};
